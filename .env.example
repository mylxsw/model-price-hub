SECRET_KEY=change-me
ADMIN_USERNAME=admin
# 使用 `python -c "from app.core.security import hash_password; print(hash_password('your-password'))"` 生成
ADMIN_PASSWORD_HASH=
DATABASE_URL=sqlite:///./app.db
